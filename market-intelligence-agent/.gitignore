# Environnement virtuel
.venv/
venv/
__pycache__/
*.pyc

# Variables d'environnement (SECRETS)
.env
.env.example

# Base de données locale (Optionnel : tu peux l'enlever si tu veux commit la DB de démo)
# customers.db

# Logs et fichiers temporaires
*.log
.DS_Store

# IDE
.vscode/
.idea/

# LangGraph SQLite checkpointer runtime files
data/checkpoints.db
data/checkpoints.db-shm
data/checkpoints.db-wal

# Agent workspace — keep the folder, ignore its contents
data/workspace/*
!data/workspace/.gitkeep
!data/workspace/screenshots/
data/workspace/screenshots/*
!data/workspace/screenshots/.gitkeep

# Claude Code local config
.claude/

# Local dev runtime logs
.logs.json
.logs_backend.txt
.logs_frontend.txt

# Root-level npm packages (e.g. CopilotKit exploration)
node_modules/
package.json
package-lock.json

# playwright-cli skill artifacts
.playwright-cli/

# QA test screenshots
qa_final_state.png
# Local git worktrees (agent-managed, not shared)
.worktrees/
