# Slim runtime requirements for AgentCore container (v1: text + MCP gateway only).
# Excludes streamlit (UI) and yfmcp/mcp-server-sqlite (those run in dedicated
# MCP Lambdas), and dev tooling. server.py unconditionally imports the voice
# stack (OpenAI GPT-Live), so its deps stay in this file.

# Web framework + ASGI
fastapi>=0.135.0
uvicorn>=0.38.0
httpx>=0.28.1
python-multipart>=0.0.20

# LangGraph + LangChain stack — pinned EXACTLY to the versions in uv.lock so
# the prod image runs what the local test suite ran. When upgrading: bump in
# uv.lock (uv lock --upgrade-package ...), run the tests, then copy the new
# versions here. Transitive family members are pinned below too.
langgraph==1.2.12
langchain==1.4.2
langchain-core==1.6.4
langchain-openai==1.6.6
langchain-pinecone==0.2.13
langchain-mcp-adapters==0.3.2
langchain-community==0.4.2
langgraph-checkpoint-aws==1.2.3
langchain-classic==1.0.8
langchain-text-splitters==1.1.2
langgraph-checkpoint==4.2.0
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.5
langsmith==0.14.0

# MCP client (talks to Gateway over streamable_http)
mcp>=1.22.0

# LLM + retrieval
openai>=3.13.0
tiktoken>=0.12.0
pinecone>=7.3.0
tavily-python>=0.7.13

# Tools
emails>=0.6

# Config + secrets
pydantic>=2.12.4
pydantic-settings>=2.12.0
python-dotenv>=1.2.1
boto3>=1.35.0

# AgentCore Browser — SigV4 WebSocket client + Playwright (managed Chromium
# runs in AgentCore Browser, so no Chromium binary install in this image).
bedrock-agentcore>=1.11.0
playwright>=1.46.0
