{
  "tools": [
    {
      "name": "read_text_file",
      "description": "Read a UTF-8 text file from the workspace.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {"type": "string", "description": "Relative path inside the workspace, e.g. brief.md"}
        },
        "required": ["path"]
      }
    },
    {
      "name": "list_directory",
      "description": "List entries (files and subdirectories) under a directory in the workspace.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {"type": "string", "description": "Relative directory path; empty for root", "default": ""}
        }
      }
    },
    {
      "name": "write_file",
      "description": "Write a UTF-8 text file to the workspace, creating or overwriting.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {"type": "string"},
          "content": {"type": "string"}
        },
        "required": ["path", "content"]
      }
    }
  ]
}
