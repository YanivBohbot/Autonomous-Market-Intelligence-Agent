[
  {
    "name": "yfinance_get_ticker_info",
    "description": "Get summary info about a stock ticker: name, sector, market cap, current price, key ratios.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "ticker": {"type": "string", "description": "Stock symbol, e.g. AAPL"}
      },
      "required": ["ticker"]
    }
  },
  {
    "name": "yfinance_get_price_history",
    "description": "Get daily OHLCV price history for a ticker over a period.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "ticker": {"type": "string"},
        "period": {
          "type": "string",
          "description": "1d, 5d, 1mo (default), 3mo, 6mo, 1y, 2y, 5y, 10y, ytd, max"
        }
      },
      "required": ["ticker"]
    }
  },
  {
    "name": "yfinance_get_ticker_news",
    "description": "Get recent news articles about a ticker.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "ticker": {"type": "string"},
        "limit": {"type": "integer", "description": "Max number of articles to return (default 5)"}
      },
      "required": ["ticker"]
    }
  }
]
